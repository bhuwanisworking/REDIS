Redis is in memory database and key-value stored (fast as stored in RAM)
and Tradational DB store the whole object  (cheap and slower as it is tored in harddisk)
Redis is very expensive thats why not used as main DB and if reboot or restarted the data is gone from redis
